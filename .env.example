# Database
DATABASE_URL="file:./dev.db"

# E2E Testing Database (completamente separada de desarrollo)
# ⚠️ IMPORTANTE: NO descomentar esta línea en .env.local
# Playwright la pasa automáticamente al ejecutar tests E2E
# Solo se usa cuando ejecutas: npm run test:e2e o npm run test:e2e:prod
# DATABASE_URL_E2E="file:./e2e.db"

# Admin (for manual scraping)
# Generate a secure 32+ character key
ADMIN_API_KEY="generate-a-secure-32-character-key-here-please"

# Environment
NODE_ENV="development"

# Browser Cache (optional - set to a directory with more space if home FS is limited)
# Playwright Cache
# Example: PLAYWRIGHT_BROWSERS_PATH="/path/to/project/cache/playwright"
PLAYWRIGHT_BROWSERS_PATH=""

# Puppeteer Cache (for Puppeteer-based scrapers like Movistar Arena)
# Example: PUPPETEER_CACHE_DIR="/path/to/project/cache/puppeteer"
PUPPETEER_CACHE_DIR=""

# Public Variables (exposed to client - prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="EnVivo"
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

# E2E Testing
E2E_BASE_URL="http://localhost:3000"
